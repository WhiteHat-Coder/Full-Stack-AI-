# Validation skill

Validate static loading, repository-subpath asset paths, responsive layout, keyboard navigation, localStorage behavior, search, and content traceability. Run focused checks after each meaningful change.
