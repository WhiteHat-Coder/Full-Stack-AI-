# Accessibility skill

Use semantic HTML, labels, keyboard support, visible focus, logical heading order, sufficient contrast, and text alternatives. Never communicate important state with color alone.
